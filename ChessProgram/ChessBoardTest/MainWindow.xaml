<Window x:Class="ChessBoardTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ChessBoardTest"
        Title="MainWindow" Height="620" Width="640" 
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
            <Button x:Name="cancelButton" Width="50" Click="cancelButton_Click"
                    Content="Undo" Margin="5" IsEnabled="False" />
            <Button x:Name="redoButton" Width="50" Content="Redo"
                    Click="redoButton_Click" Margin="5" 
                    IsEnabled="False" />
        </StackPanel>
        <local:ChessBoardControl x:Name="chessBoardControl" 
                                 Grid.Row="2" Grid.Column="1" />
    </Grid>
</Window>
